/**
 * Frontend entry point — styles and scripts loaded on the front end.
 * Webpack extracts CSS to build/frontend.css.
 * JS is output to build/frontend.js. Depends on jQuery (enqueued via WP).
 */
import "./scss/main.scss";
